name: Atualizar Badges (Via API JSON)

on:
  schedule:
    - cron: "0 0 * * *" # Roda todo dia à meia-noite
  workflow_dispatch: # Permite rodar manualmente

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Instalar requests
        run: pip install requests

      - name: Rodar Script de Atualização
        shell: python
        run: |
          import requests
          import sys

          # --- CONFIGURAÇÃO ---
          URL = "https://www.credly.com/users/louise-aldrighi/badges.json"
          
          print(f"--- CONSULTANDO API JSON ---")
          print(f"Alvo: {URL}")
          
          try:
              headers = {
                  "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/115.0.0.0 Safari/537.36"
              }
              
              response = requests.get(URL, headers=headers)
              
              if response.status_code != 200:
                  print(f"❌ Erro ao acessar a API: {response.status_code}")
                  sys.exit(1)
              
              data = response.json()
              badges_list = data.get('data', [])
              
              print(f"✅ Sucesso! Encontrados {len(badges_list)} badges.")

              if not badges_list:
                  print("Aviso: Lista de badges retornou vazia.")
                  sys.exit(0)

              # --- MONTAGEM DO HTML ---
              html_content = '<div align="center">\n'
              
              # Pega as 8 mais recentes
              count = 0
              for item in badges_list:
                  if count >= 8: break 
                  
                  badge_details = item.get('badge_template', {})
                  
                  # Dados para o HTML
                  badge_id = item.get('id')
                  titulo = badge_details.get('name', 'Badge')
                  img_url = badge_details.get('image', {}).get('url')
                  
                  # Constrói o link público do Credly
                  public_link = f"https://www.credly.com/badges/{badge_id}"
                  
                  if img_url and badge_id:
                      # Adiciona a imagem com link
                      html_content += f'  <a href="{public_link}" title="{titulo}"><img src="{img_url}" width="110" style="margin: 5px;"></a>\n'
                      count += 1
              
              html_content += '</div>'
              
              # --- ATUALIZAÇÃO DO README ---
              print("Lendo README.md...")
              with open('README.md', 'r', encoding='utf-8') as f:
                  readme = f.read()
              
              # AQUI ESTAVA O ERRO: As strings não podem estar vazias
              start_marker = ""
              end_marker = ""
              
              if start_marker not in readme or end_marker not in readme:
                  print("❌ ERRO: Marcadores não encontrados no README.")
                  sys.exit(1)
              
              # Substitui o miolo entre os marcadores
              part1 = readme.split(start_marker)[0]
              part2 = readme.split(end_marker)[1]
              
              new_readme = f"{part1}{start_marker}\n{html_content}\n{end_marker}{part2}"
              
              with open('README.md', 'w', encoding='utf-8') as f:
                  f.write(new_readme)
              
              print("✅ README atualizado com sucesso!")

          except Exception as e:
              print(f"❌ Ocorreu um erro crítico: {e}")
              sys.exit(1)

      - name: Commit e Push das alterações
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "🤖 Update: Badges via API JSON"
